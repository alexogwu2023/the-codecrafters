Comments in Go are group of text that are expected to be ignored by the compiler.
We have two types of comments: we have the single line comments and multi line comments.
Single line comments start with two forward slashes (//). While multi line comments starts with a forward slash and an astericks and ends with the same (/* */)